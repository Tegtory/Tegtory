name: Deploy DEV
on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
        with:
          clean: false
          path: 'prod'
      - run: |
          cd prod
          docker compose -f docker/docker-compose.dev.yml up -d --build
